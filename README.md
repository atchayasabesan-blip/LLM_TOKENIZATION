LLM Tokenization: BERT vs GPT-2
This project demonstrates how different Large Language Model (LLM) tokenizers process the same text.
The project focuses on comparing **BERT WordPiece Tokenization** and **GPT-2 Byte Pair Encoding (BPE) Tokenization** and analyzing the difference in tokenization and token counts.
